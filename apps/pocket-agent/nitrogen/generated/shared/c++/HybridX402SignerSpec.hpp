///
/// HybridX402SignerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <optional>
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::x402signer {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `X402Signer`
   * Inherit this class to create instances of `HybridX402SignerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridX402Signer: public HybridX402SignerSpec {
   * public:
   *   HybridX402Signer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridX402SignerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridX402SignerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridX402SignerSpec() override = default;

    public:
      // Properties
      virtual bool getIsSecureHardwareAvailable() = 0;

    public:
      // Methods
      virtual std::string generateSoftKey(const std::string& alias, const std::string& curve) = 0;
      virtual std::optional<std::string> softPublicKey(const std::string& alias) = 0;
      virtual std::string signSoftDigest(const std::string& alias, const std::string& digestHex) = 0;
      virtual std::string signSoftMessage(const std::string& alias, const std::string& messageHex) = 0;
      virtual void deleteSoftKey(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<bool>> authenticate(const std::string& reason) = 0;
      virtual std::string generateEnclaveKey(const std::string& alias, bool requireBiometry) = 0;
      virtual std::optional<std::string> enclavePublicKey(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<std::string>> signEnclaveDigest(const std::string& alias, const std::string& digestHex) = 0;
      virtual void deleteEnclaveKey(const std::string& alias) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "X402Signer";
  };

} // namespace margelo::nitro::x402signer
